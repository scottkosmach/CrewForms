# =============================================================================
# Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# NEVER commit .env.local to git - it contains secrets!
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# Get these from: Supabase Dashboard -> Project Settings -> API
# -----------------------------------------------------------------------------

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Public/Anon key (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role key (server-side only - NEVER expose to client)
# Only needed for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# App Configuration
# -----------------------------------------------------------------------------

# Your app's URL (used for auth redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Comma-separated emails allowed to WRITE global data (site mappings, Excel
# templates) through the API. When unset, any signed-in user may write —
# fine while there is exactly one user.
ADMIN_EMAILS=you@example.com
